# Usage

- install packages  ```jsonwebtoken lru-cache @irn/irn-backoffice-integration @irn/irn-backoffice-design-system```
- required variables ```IRN_API_BACKOFFICE_BASE_URL JWT_KEY JWT_PRIVATE_KEY```
- use irn-layout and wrap the whole app 
- 